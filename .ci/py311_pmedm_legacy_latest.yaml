#########################################################################
# Install all versions from ** conda through micromamba where possible **
#########################################################################

name: py311_pmedm_legacy_latest
channels:
  - conda-forge
  - r
dependencies:
  - python=3.11
  # core dependencies -- not including `livelike`
  - numpy
  - pandas
  - scipy
  # testing & development
  - git
  - pip
  - pre-commit
  - pyarrow
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest
  - ruff
  - setuptools_scm
  - watermark
  # r packages -- not including `jovtc/pmedmrcpp`, `pmedmrcpp-pal`
  - rpy2
  - r-base
  - r-rcpp
  - r-rcppeigen
  - r-matrix
  - r-remotes
  - r-irkernel
